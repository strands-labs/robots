# Whole-body bed-making RL: a free-base Unitree G1 (Inspire 5-finger hands) learns to
# balance while reaching a commanded hand target over the bed — the loco-manipulation
# skill a pure walking policy can't hold. Trained on Isaac Lab's native manager-based RL
# rails (rsl-rl-lib 5.x), physically valid (no base pinning / teleporting / joint freezing),
# and deployable on the same Inspire-hand G1 the demo renders.
