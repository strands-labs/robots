### Fixed

- **strings**: tool-result text now renders in ASCII, and the guard that enforces the ASCII rule grades that surface. AGENTS.md names three surfaces - tool result dicts, log messages, error messages - and `tests/test_log_strings_are_ascii.py` graded the latter two, so ten typographic glyphs sat in text an agent reads back out of its own tool call: `U+2194` in `get_contact_forces` (which `simulation.mujoco.rendering` already spelled `<->` for the same contact pair), `U+2192` in `set_geom_properties` / `set_body_properties` / the GR00T checkpoint download, `U+00D7` in `get_mass_matrix`, `U+00B7` in `apply_force`'s torque unit, `U+00B1` in `randomize`, and `U+2022` in `list_benchmarks`. Each becomes the spelling the package already uses for the same content. Every action's status and JSON payload block is unchanged.
