### Fixed

- **docs**: joining the Zenoh mesh is opt-in (`mesh` defaults to `None`, which consults `STRANDS_MESH`, and unset leaves it off), but `README.md`, `docs/mesh.md` and `docs/security.md` still described the pre-#630 default in three spellings. Eleven `.mesh` reads across four fenced blocks ran on robots that never opted in - the mesh guide's own two-process example raised `AttributeError: 'NoneType' object has no attribute 'peers'` on its fourth line - four paragraphs said a bare `Robot()` joins by itself, and README's parameter table documented the default as `True`, so the reader's only documented lever (`mesh=False`) was a no-op for the state they were in. Correct all three pages, and add `test_docs_mesh_join_is_opt_in` to grade every documentation page at the scope each spelling lives in: per fenced block, per paragraph, per table row.
