### Fixed

- **CI**: the `Install system dependencies (OpenGL for MuJoCo)` step declines apt Recommends, so it no longer downloads 29.9 MiB (30.5%) it cannot use and stops being reaped by its own 12-minute bound. Two unrelated pull requests were killed 40 minutes apart on 2026-08-19 by a stalling Ubuntu mirror, neither of them touching CI, MuJoCo or video. Dropped: VA-API/VDPAU hardware video-acceleration drivers on a runner with no GPU, Blu-ray DRM, a GTK decoration plugin, an SVG loader, and `pocketsphinx-en-us` — 26.2 MiB of speech-recognition acoustic models reached through `ffmpeg` → `libavfilter9` → `libpocketsphinx3`, and the largest single file in the set. `ffmpeg` and every `libav*` library stay, since they carry the `h264`/`hevc`/`libsvtav1` software encoders and `torchcodec` links them.
