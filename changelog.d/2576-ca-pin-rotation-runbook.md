### Fixed

- **mesh/iot**: publish the CA pin rotation runbook that the Amazon Root CA1 pin's own artifacts cite. `AGENTS.md` names `README.md > "CA Pin Rotation Runbook"` as where the procedure lives and `tests/mesh/test_iot_ca_pin_rotation.py` says its assertions pin the state that runbook depends on, but README carried no such heading - so the ordered procedure existed only in a contributor file and a test docstring. Both pin refusals also named no procedure, and the on-disk advice (delete and re-download) leads to the download path, which refuses the same bytes for the same reason. The runbook is now published under the cited heading, stating the already-decided strategy (verify out of band, ship a release carrying both pins, wait for fleet uptake, drop the old pin in a follow-up, `STRANDS_MESH_CA_PINS` as the emergency overlap and not the break-glass), and all three refusals name it through one owner so it is reachable from the failure that needs it.
