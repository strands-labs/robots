### Added

- User-facing dashboard docs land as their own page tree under `docs/dashboard/`:
  quickstart, index, remote access, code-defined robots, collect-train-deploy,
  troubleshooting. The pages describe what the operator dashboard already ships;
  no code changes.
