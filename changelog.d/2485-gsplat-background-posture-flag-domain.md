### Fixed

- **rendering**: `GsplatBackground` now checks its `auto_backdrop`, `skybox`, `metric` and `own_floor` posture flags on the shared boolean domain instead of reading them by truthiness. Each selects an alignment or compositing branch, so every spelling of *off* a caller reaches for - `"false"`, `"no"`, `"off"`, `"0"` - is a truthy string that selected the branch the value asks to skip, while `0`, `""`, `[]` and `None` took the default branch without being a spelling of it. `metric` also decides whether `radius` is read at all, so `metric="no"` kept a capture's raw scale and placed a real 500k-splat room at a 4.45 m radius for a caller who asked for 2.5 m, with nothing raised or logged. The refusal names the flag and the value, and the constructor's `Raises:` now documents all three of its verdicts.
