### Fixed: the Training overview's data-loop fence now loads the checkpoint it trains. Step 4 of the fence called `create_policy(ckpt)` on the freshly trained directory and, on a clean install, `lerobot_local` refused it with `UntrustedRemoteCodeError` because the fence never set `STRANDS_TRUST_REMOTE_CODE`. The fence now sets the opt-in right before the load, the same way `examples/07_post_tune_any_policy.py` does, so the recorded-trained-deployed loop runs end to end as written.
