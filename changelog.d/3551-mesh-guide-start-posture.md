### Fixed: the mesh guide's opening block now starts a mesh on a fresh install. `docs/mesh.md` opened with two processes calling `Robot(..., mesh=True)` and no security posture set, so following it verbatim ended in `Mesh did NOT start` and a `robot.mesh` that was never alive; the page never named the variable that lets it start. The block is now preceded by `export STRANDS_MESH_LOCAL_DEV=1` for localhost experiments, with the lab and production postures pointed at on the Security page, and a test replays the page's environment against the same gate `Mesh.start` asks.
